# Base URL for the backend API.  During development the Pyramid backend runs
# on port 6543.  Override this value if your backend listens on a different
# host or port.
VITE_API_URL=http://localhost:6543
